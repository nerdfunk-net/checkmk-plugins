# Checkmk agent configuration for the directory_status plug-in.
# Place this file as $MK_CONFDIR/directory_status.cfg (usually /etc/check_mk/).
#
# List one absolute directory path per line. Lines starting with # are ignored.
#
# /var/spool/incoming
# /tmp/uploads
